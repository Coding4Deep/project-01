# Spring Boot Application Configuration - Local Profile
spring.application.name=DevOps Portal

# Server Configuration
server.port=8080
server.servlet.context-path=/

# MongoDB Configuration (Environment Variables)
spring.data.mongodb.host=${MONGO_HOST:localhost}
spring.data.mongodb.port=${MONGO_PORT:27017}
spring.data.mongodb.database=${MONGO_DATABASE:devopsdb}

# RabbitMQ Configuration (Environment Variables)
spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USERNAME:rabbitmq}
spring.rabbitmq.password=${RABBITMQ_PASSWORD:rabbitmq}
spring.rabbitmq.virtual-host=${RABBITMQ_VHOST:/}

# Memcached Configuration (Environment Variables)
memcached.servers=${MEMCACHED_SERVERS:localhost:11211}

# Logging Configuration
logging.level.com.example.devopsapp=INFO
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG

# Thymeleaf Configuration
spring.thymeleaf.cache=false
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Security Configuration
spring.security.user.name=admin
spring.security.user.password=admin123

# Actuator Configuration
management.endpoints.web.exposure.include=health,info,metrics
management.endpoint.health.show-details=always
management.info.env.enabled=true

# Session Configuration
server.servlet.session.timeout=30m
server.servlet.session.cookie.http-only=true
server.servlet.session.cookie.secure=false

# Error Handling
server.error.whitelabel.enabled=false
server.error.path=/error
