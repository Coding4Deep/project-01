apiVersion: v1
kind: Service
metadata:
  name: kafka
  labels:
    app: kafka
spec:
  selector:
    app: kafka
  ports:
  - name: kafka-internal
    port: 29092
    targetPort: 29092
    protocol: TCP
  - name: kafka-external
    port: 9092
    targetPort: 9092
    protocol: TCP
  clusterIP: None
